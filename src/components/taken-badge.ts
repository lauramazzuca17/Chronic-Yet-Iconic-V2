/**
 * Shared Taken pill — Figma Home water card + Log electrolytes.
 * Water's "Today's Total" stays on Log (different width/value).
 */

export const TAKEN_BADGE = {
  bg: "#efefef",
  radiusPx: 4,
  widthPx: 65,
  padXPx: 10,
  padYPx: 6,
  gapPx: 2,
  labelColor: "#5c5c60",
  labelSizePx: 14,
  labelLineHeightPx: 18,
  iconSizePx: 22,
  checkSrc: "/icons/check-square.svg",
  xSrc: "/icons/x-square.svg",
} as const;
